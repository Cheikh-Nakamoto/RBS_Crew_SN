import { PresseCreateForm } from '../_components/presse-create-form';

export const metadata = { title: 'Nouvelle mention presse' };

export default function NouvellePressePage() {
  return <PresseCreateForm />;
}
